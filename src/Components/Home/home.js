import React, { Component } from 'react';

const Home = ({data}) => {
    return (
        <div className="">
            <h1>Home</h1>
        </div>
    );
};

class HomePage extends Component {
  render() {
    return (
        <Home />
    );
  }
};

export default HomePage;